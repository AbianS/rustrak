# Session 2026-06-29

## Request
Abian: analyze Rustrak current state + issue #143, check Sentry source, decide which
named item types are missing AND important to have.

## Findings
- Rustrak `EnvelopeItemKind` (envelope.rs:52): Event, Transaction, Session, Sessions, **Log**, Other.
  - My BOND was stale: **Log is now implemented** (v0.8.0, `digest/processors/logs.rs`). Issue #143
    item #7 is DONE.
- Relay `ItemType` enum (relay-server/src/envelope/item.rs:788, SHA 97f9c4b): 24 variants.
  - relay SHA changed externally 4222d43 → 97f9c4b. New processing dirs since: `user_reports`,
    `trace_attachments`, `trace_metrics` (dedicated now).

## Verdict (priority of #143's 8 still-missing items)
- 🟢 DO NOW: **CheckIn** — lowest complexity (`processing/check_ins/`: normalize + rate limit),
  no grouping, standalone product feature, broad SDK reach. Best ROI.
- 🟡 STRATEGIC/EXPENSIVE: **Span (standalone)** — issue's P0, OTel future, but heavy pipeline +
  requires DSC parsing from `trace` header (Rustrak ignores it entirely). Pushes Rustrak into APM.
- 🔴 DEFER: Profile/ProfileChunk (needs solid transactions), Replay (large blob storage, anti-lightweight).
- 🔴 SKIP: Metrics Statsd/MetricBuckets (custom metrics was a sunset Sentry beta — product knowledge,
  NOT confirmable from clone; no deprecation comment found in item.rs/relay-metrics), TraceMetric (niche),
  Integration (is_internal, needs Span+Log first).

## Key insight: issue #143 scope gap
#143 only covers performance items. For an *error tracker*, these (NOT in #143, all fall into `Other`)
matter MORE than half its list:
- **Attachment** (item.rs:796) — screenshots/logs/minidumps on crashes, huge SDK reach.
- **User Feedback** (UserReportV2/"feedback" + UserReport, item.rs:803) — dedicated `processing/user_reports/`.
- **Security/RawSecurity** (CSP), **ClientReport** (client-side dropped-event outcomes → accurate stats).

Recommended to Abian: CheckIn first; open a separate issue for Attachments + User Feedback.

## Open follow-ups offered (not yet done)
- Trace full CheckIn pipeline in Relay with permalinks for implementation planning.
- Draft Attachments/Feedback issue.
