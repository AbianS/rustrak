## Session — verifying PR #169 review-fix pass (issue status/substatus CHECK constraints)

**What happened:** Abian's coordinator agent had just applied 30 fixes from a CodeRabbit/Greptile review pass on PR #169 (Sentry-compatible issues API), including new Postgres/SQLite CHECK constraints on `status`/`substatus`/`priority`/`assignee_type` and a Rust-side `UpdateIssueState::validated_substatus()` guard. Asked to sanity-check the changes against real Sentry/Relay behavior before commit.

**Key findings:**
- The new substatus CHECK constraint + `validated_substatus()` enumerated only 6 of the 7 canonical values already documented in this repo's own `docs/sentry-compat/issue-165-roadmap.md:75` — `escalating` (standalone value, distinct from `archived_until_escalating`) was silently dropped from both the SQL CHECK list and the Rust match arm.
- Root cause traced to BOND.md itself: the original #165 audit entry said "5-status+6-substatus" — that miscount (should be 7 substatus values) is exactly the gap the new constraint reproduced. Fixed the stale BOND.md count too.
- No live bug today — grep confirms zero call sites anywhere in `apps/server/src` ever set `substatus = "escalating"`. But the CHECK constraint would have hard-blocked any future escalation-detection feature with an opaque DB constraint violation, not an obvious error pointing back to the roadmap doc.
- `assignee_type IN ('user','team')` (also newly added in this pass) could not be verified against `relay-repo` or `sentry-data-schemas` — issue assignment is Sentry-monolith product logic, out of Relay's ingestion scope. No contradicting evidence, zero current call sites either way (Rustrak has no teams table). Flagged as unverified-but-low-risk rather than confirmed.
- Everything else in the batch checked out: `status` enum (unresolved/resolved/ignored) and `priority` enum (low/medium/high) both match the roadmap exactly; `resolvedInNextRelease` bulk handling mirrors the pre-existing single-issue special case (a real Sentry Update Issue API status value, not invented); the new `IssueResponse` enrichment fields follow this project's existing snake_case convention, not a Sentry wire-format claim, so no protocol conflict there.

**Rustrak gaps confirmed:** substatus CHECK/validator missing `escalating` — fixed in this session (added `SUBSTATUS_ESCALATING` const, updated both migration CHECK lists and `validated_substatus()`). Recompiled + clippy + full test suite clean after the fix.

**Follow-up:** `assignee_type` enum values still unverified against primary Sentry source — if/when teams or assignment become a real feature, re-check against actual Sentry `GroupAssignee` semantics rather than trusting the two-value guess. relay-repo also has a pending upstream update (origin/master at ded5f96f8, local SHA 97f9c4b) noted in BOND.md — not pulled this session, unrelated to this check.

---

## Session — full pre-merge critical audit of feat/issue-165-issues-sentry-compat

**What happened:** Abian asked for a critical, read-only review of the entire branch (76 files, ~8.7k lines: issue lifecycle/status model, issue_social.rs, bulk operations, routes/issues.rs, client+MCP packages, webview-ui redesign) before merging to main. Ran 5 parallel fork audits (grouping/digest, issue lifecycle/social, API routes + client/MCP contracts, webview UI ×2 — first UI fork went rogue and spawned an untracked nested sub-agent that never reported back, had to relaunch with explicit "do not delegate" instruction). Verified two loose ends myself directly (page-container max-w removal blast radius, issue-activity.tsx).

**Key findings:**
- **New blocker, same bug class as this morning's `escalating` gap but one layer up:** `validated_substatus()` (models/issue.rs:183-200) validates substatus membership in the 7-value set only — never cross-checks compatibility with the accompanying `status`. `IssueService::set_status` (services/issue.rs:658) lets `substatus_override` win unconditionally over the status-derived default (`substatus_override.or(default_substatus)`). Two independent forks confirmed this via the same repro: `PATCH {"status":"resolved","substatus":"escalating"}` persists an invalid pair — no CHECK constraint stops it, since both migrations only validate `substatus` in isolation, never `(status, substatus)` jointly. Not exploitable today (no escalation-detection feature exists to trigger it automatically) but directly reachable via the public PATCH API. Same root-cause shape as the `escalating`-missing bug fixed this morning: partial validation that looks complete.
- Bulk endpoints (`PUT`/`DELETE /issues`) have no cap on `ids: Vec<Uuid>` size (models/issue.rs:142-153) — DoS surface. The bulk-priority path additionally does `get_by_id` + `set_priority` per id with no batching and no shared transaction (routes/issues.rs:466-479), unlike `bulk_set_status` which at least wraps its loop in one tx.
- Bulk operation tests bypass the real HTTP handlers entirely — `digest_test.rs:1188` calls `IssueService::bulk_set_status`/`bulk_delete` directly, so `access::require`, the `resolvedInNextRelease` branch, and the priority-loop logic have zero integration coverage. Client/MCP tests are happy-path only (no oversized batch, empty list, duplicate ids, or cross-project ids tested).
- Everything else checked out clean: fingerprint `LenientString` coercion and `mechanism.synthetic` grouping logic both verified correct against relay-repo permalinks (SHA 97f9c4b), no regressions. Migrations symmetric across Postgres/SQLite. `issue_social.rs` fully parameterized SQL, project-scoping enforced via `require_issue_in_project` at the route layer. Authz/IDOR: every bulk and single mutation correctly scopes by `project_id` — no cross-tenant path found. Contract consistency (status/priority enums) matches across server/openapi/client-zod/MCP. Webview UI: event detail page, navigation/collapsible-rail, issues-list layout, and status/substatus UI coverage (`issue-status.ts` — correctly has all 7 including standalone `escalating`, my suspicion it'd be missing there was wrong) all safe. `page-container max-w` removal (commit 75e1223) is deliberate and applied consistently across all 8 affected pages per its own commit message — not a regression.
- Minor: `issues-list.tsx` dropdown visibility (Resolve/Archive) still reads legacy `is_resolved`/`is_muted` booleans instead of the canonical `status` enum for gating — works today only because the server keeps them mutually derived; a landmine if the compat booleans are ever dropped.
- Nit: grouping.rs truncates `release` to 250 chars / `logger` to 128 chars vs Relay's actual 200/64 — cosmetic over-permissiveness, not a correctness bug.
- Operational lesson: a fork given a broad multi-part task quietly delegated part of it to a nested sub-agent and then terminated before that child reported back — the result was silently lost since I had no tracking handle on the grandchild. Fix: when forking, explicitly instruct "do the work yourself, do not spawn further agents" for any task where losing partial results would matter.

**Rustrak gaps confirmed:**
1. ❌ `(status, substatus)` pair not validated together — blocker, needs a fix before merge (validate the pair in `set_status`, or a compound CHECK).
2. ⚠️ Bulk endpoints uncapped `ids` size + N+1 priority loop — major, DoS risk, should cap before merge (Sentry's own dashboard bulk-action UI caps around 100).
3. ⚠️ Bulk operation integration tests bypass real HTTP handlers — major test-coverage gap on the IDOR-relevant code path.
4. Minor: webview `issues-list.tsx` dropdown gates on legacy booleans instead of `status` enum.
5. Nit: release/logger truncation limits don't match Relay's exact byte counts (250/128 vs 200/64).

**Follow-up:** None of these were fixed this session (read-only audit as requested). Next session should verify whether Abian fixed #1–#3 before merge, and prune this entry once resolved.

---

## Session — fixing all 5 audit findings via /tdd

**What happened:** Abian asked to fix everything from the audit above, backend work via `/tdd`. Did all 5, strict red-green-refactor for the Rust ones. Mid-session accidentally ran a `git stash` as part of a compound bash command while trying to test against a clean tree — Abian caught it immediately ("no hagas commits sin yo decirlo, desaz y continua"), I `git stash pop`'d and confirmed via diff that all work was intact before continuing. No commits were made at any point (never attempted one) — the correction was specifically about not running git commands with side effects (stash included) without asking first.

**Key findings / fixes:**
1. `(status, substatus)` pair validation — added `substatus_valid_for_status()` (models/issue.rs, unit-tested) wired into `IssueService::set_status` as the chokepoint. RED test reproduced the real bug first (`resolved`+`escalating` persisted), GREEN after fix. Verified against both SQLite and a real Postgres testcontainer.
2. Bulk `ids` cap (`MAX_BULK_IDS = 100`) + new batched `IssueService::bulk_set_priority` (dual-dialect: `ANY($1)` postgres / `QueryBuilder IN (...)` sqlite) replacing the old N+1 per-id loop. `bulk_delete` intentionally left as a per-row loop — it reuses `delete()`'s project-counter side effects, already documented as unsafe to batch.
3. Added 3 HTTP-level bulk tests to `issues_api_test.rs` (through real `routes/issues.rs` handlers, not `IssueService` directly). **Discovered while adding them:** the entire file (21 pre-existing tests + my 3, all `#[ignore]`'d) is skipped by default `cargo test`/CI, and running many of them together with `--ignored` causes ~4 pre-existing ones to fail with what looks like cross-test data bleed — even though each gets its own private SQLite `:memory:` pool. Not diagnosed (out of scope, pre-existing, not caused by this session's changes). My 3 new tests pass individually. Net: proves the HTTP wiring is correct, doesn't yet give real default-CI coverage — same limitation the file already had.
4. `issues-list.tsx` dropdown gating swapped from `is_resolved`/`is_muted` to `status === 'resolved'`/`'ignored'`. Grep-confirmed no other webview-ui file reads those booleans. `tsc --noEmit` clean. Not manually verified in a running browser.
5. `logger`/`release` truncation limits aligned to Relay's exact values (64/200, was 128/250). TDD, tests pass.

All 236 unit + 201 integration (+81 ignored) + 116 e2e/doc tests green on SQLite; new/changed logic also verified against a real Postgres container. `cargo clippy --all-targets` clean, `cargo fmt` applied. `openapi.json` not touched — none of the fixes changed a route signature or a `ToSchema`-derived struct's fields.

**Rustrak gaps confirmed:** All 5 from the prior audit session, now fixed (see BOND.md gap table, entries updated to ✅ with fix details).

**Follow-up:** The `issues_api_test.rs` file-wide `#[ignore]` + cross-test-bleed-when-run-together issue is a real, pre-existing gap worth its own investigation session — right now this whole file (24 tests) has zero real default-CI coverage despite looking like it does. Also: manual browser verification of the `issues-list.tsx` dropdown fix is still owed.

---

## Session — investigating the issues_api_test.rs failures (correcting an earlier wrong guess)

**What happened:** Abian asked to investigate the follow-up above. My earlier "cross-test data bleed" hypothesis was wrong and I hadn't actually tested it — corrected that now with real evidence instead of leaving a guess on record.

**Key findings:**
- Ran the same 4 failing tests with `--test-threads=1` (fully sequential): still fail, identically. Ran each of the 4 completely alone: still fail, identically. This rules out concurrency/shared-state entirely — no static/global mutable state exists in the relevant code paths (`access.rs`, `rate_limit.rs` checked, clean).
- Real cause: `GET /issues` was refactored from cursor pagination to offset pagination in commit `56fde56` (#165 itself, this same branch) — `git log -p -- src/pagination/mod.rs` confirms both response shapes were introduced together, and issues listing moved to `OffsetPaginatedResponse` (`items`/`total_count`/`page`/`per_page`/`total_pages`). The 4 broken tests still assert the old shape (`has_more`, `next_cursor`, `?cursor=`, `?include_resolved=true` — replaced by `?filter=all`), and assumed a 250-item default page size that's actually `PAGE_SIZE = 20` (`pagination/mod.rs:8`).
- This drift went undetected because the entire file already carries a blanket `#[ignore = "Session cookies not preserved..."]` on all 24 tests (21 pre-existing + my 3 from the prior session) — a reason that's demonstrably false for these Bearer-token tests (20 of 24 pass individually right now, and would pass in real CI). The API contract changed under a test suite that was already silently disabled, so nobody caught it.
- Not fixed — Abian asked to investigate, not fix. BOND.md gap table entry corrected with the real root cause and a scoped fix recommendation (rewrite the 4 assertions for offset pagination, then drop the file-wide `#[ignore]`).

**Rustrak gaps confirmed:** Root cause identified and documented in BOND.md (was previously mis-documented as concurrency-related — now corrected).

**Follow-up:** Ask Abian if they want the 4 tests fixed + the `#[ignore]` dropped now, in a follow-up session, or left for later. Manual browser verification of the `issues-list.tsx` dropdown fix (from the prior session) is still owed too.

---

## Session — fixing the 4 tests + dropping the file-wide #[ignore]

**What happened:** Abian confirmed: fix the 4 tests to match the offset-pagination contract and drop `#[ignore]`. Did it.

**Fixes:**
- `test_list_issues_empty`: asserts `total_count`/`total_pages` instead of `has_more`/`next_cursor`.
- `test_list_issues_include_resolved`: `?include_resolved=true` → `?filter=all`.
- `test_list_issues_pagination`: now creates 25 issues and actually exercises 2 pages (20 + 5), asserting `total_count`/`page`/`per_page`/`total_pages` on both.
- `test_list_issues_invalid_cursor` → renamed `test_list_issues_invalid_page_param`: `?cursor=` (dead param) replaced with `?page=not_a_number`, asserting the 400 actix already gives for free on `Query` extraction failure.
- Removed `#[ignore = "Session cookies not preserved..."]` from all 24 tests in the file (21 pre-existing + 3 added last session).

**Verification:** all 24 pass together, repeated 3x for stability, and again against a real Postgres testcontainer. Full suite: 225 integration passed (was 201), 57 ignored (was 81, all genuinely still-relevant elsewhere in the codebase — e.g. sourcemap worker retry tests). `cargo clippy --all-targets` clean, `cargo fmt` applied. No commits made.

**Rustrak gaps confirmed:** Both remaining rows from the prior investigation now ✅ Fixed in BOND.md.

**Follow-up:** Manual browser verification of the `issues-list.tsx` dropdown fix (two sessions ago) is still owed — nobody's asked for it yet, noting it stays open.
