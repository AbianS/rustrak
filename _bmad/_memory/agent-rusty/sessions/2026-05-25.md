## Session — PR #95 review feedback analysis (feat/source-map-processing)

**What happened:** Called to verify whether greptile-apps bot was correct that `uploadChunks()` in the TypeScript client was broken because it used `'file'` as the multipart field name instead of the SHA-1 hash.

**Key findings:**
- BOND.md already had the answer: chunk upload field name was a known gap, fixed on 2026-05-25. Server validates `sha1_hex == field_name` (routes/sourcemaps.rs:183). Real sentry-cli uses SHA-1 as field name.
- The TypeScript client (`packages/client/src/resources/sourcemaps.ts:50`) was NOT updated when the server fix was applied — it still sent `form.append('file', chunk)`, always resulting in 400.
- Fix applied: changed `uploadChunks` signature from `Blob[]` to `{ hash: string; data: Blob }[]`, uses `chunk.hash` as field name. Tests updated. All 280 tests pass.
- Second fix: `sourcemaps_api_test.rs:2005` — `&[sha.clone()]` → `std::slice::from_ref(&sha)` for new `clippy::cloned_ref_to_slice_refs` lint in Rust 1.95 (stable).

**Rustrak gaps confirmed:** none new — already tracked.

**Follow-up:** none.
