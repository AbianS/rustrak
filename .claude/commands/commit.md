---
description: Create a concise git commit
allowed-tools: Bash(git add:*), Bash(git commit:*)
model: haiku
---

# Commit changes

<git_diff>
!`git diff --cached`
</git_diff>

Write a single-line, conventional commit-style message.
Maximum 72 characters, no extra paragraphs, no footers.