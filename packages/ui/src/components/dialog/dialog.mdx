import { Canvas, Controls, Meta, Unstyled } from '@storybook/addon-docs/blocks';
import {
  Cover,
  DocPage,
  Guarantee,
  Guarantees,
  P,
  Place,
  Rule,
  Section,
  UsedIn,
} from '../../docs/blocks';
import { Text } from '../text/text';
import * as DialogStories from './dialog.stories';

<Meta of={DialogStories} />

<Unstyled>
<DocPage>

<Cover
  eyebrow="Components"
  title="Dialog"
  lede="The modal. It stops the work and does not give it back until something is decided."
  note="Header, body with its own scroll, footer with the primary action on the right. The only thing that varies is the width."
/>

<Section
  n="01"
  title="It opens only when there is something to decide"
  intro="What resolves on the screen stays on the screen; what is told and leaves goes to a corner toast."
>

<Canvas of={DialogStories.Composed} sourceState="none" />

<Rule>
  If a dialog has nothing to decide, it is a toast. If it has too much, it is a
  page.
</Rule>

</Section>

<Section
  n="02"
  title="Four widths, and the width follows the purpose"
  intro="Not the amount of content: the kind of decision."
>

<div className="grid grid-cols-1 gap-px overflow-hidden rounded-md border border-border-subtle bg-border-divider sm:grid-cols-2">
  <div className="flex flex-col gap-1 bg-panel p-4">
    <Text variant="mono-sm" tone="brand">sm · 420 px</Text>
    <Text variant="meta" tone="secondary">Confirming something reversible.</Text>
  </div>
  <div className="flex flex-col gap-1 bg-panel p-4">
    <Text variant="mono-sm" tone="brand">md · 480 px</Text>
    <Text variant="meta" tone="secondary">The irreversible, which also asks to be typed to unlock.</Text>
  </div>
  <div className="flex flex-col gap-1 bg-panel p-4">
    <Text variant="mono-sm" tone="brand">lg · 620 px</Text>
    <Text variant="meta" tone="secondary">A short form, or the outcome of a long process.</Text>
  </div>
  <div className="flex flex-col gap-1 bg-panel p-4">
    <Text variant="mono-sm" tone="brand">xl · 1040 px</Text>
    <Text variant="meta" tone="secondary">Choosing in a table: a project, a release, a member.</Text>
  </div>
</div>

</Section>

<Section
  n="03"
  title="confirm and alert"
  intro="Opened by calling and awaited, so the caller reads as the sentence it is: if the answer is no, stop."
>

<Canvas of={DialogStories.Confirm} sourceState="none" />

<P>The destructive confirmation that cannot be undone asks for the name to be typed. It is the one place <code>danger-primary</code> appears, because by then the dialog has already said what the action takes with it.</P>

<Canvas of={DialogStories.ConfirmWithPhrase} sourceState="none" />

</Section>

<Section n="04" title="Where it is used">

<UsedIn>
  <Place at="Deleting anything">confirm, with the phrase for a project.</Place>
  <Place at="Issue detail">The ignore rules, which are a short form.</Place>
  <Place at="Project switcher">xl, choosing from a table.</Place>
  <Place at="Errors that block">alert, when there is nothing to decide but the work cannot go on.</Place>
</UsedIn>

</Section>

<Section n="05" title="What it guarantees">

<Guarantees>
  <Guarantee story="Composed">Focus is trapped while it is open and returns to whatever opened it when it closes.</Guarantee>
  <Guarantee story="Confirm">Escape and the backdrop both answer “no”, so the safe answer is the easy one.</Guarantee>
  <Guarantee story="ConfirmWithPhrase">The unlock phrase is compared exactly; the primary stays disabled until it matches.</Guarantee>
  <Guarantee story="AwaitedPicker">It is awaited: the caller gets the answer as a value, not as a callback.</Guarantee>
</Guarantees>

</Section>

<Section n="06" title="Props">

<Controls />

</Section>

</DocPage>
</Unstyled>
