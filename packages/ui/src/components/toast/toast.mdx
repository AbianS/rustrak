import { Canvas, Controls, Meta, Unstyled } from '@storybook/addon-docs/blocks';
import {
  Cover,
  DocPage,
  Guarantee,
  Guarantees,
  P,
  Place,
  Rule,
  Section,
  UsedIn,
} from '../../docs/blocks';
import { Text } from '../text/text';
import * as ToastStories from './toast.stories';

<Meta of={ToastStories} />

<Unstyled>
<DocPage>

<Cover
  eyebrow="Components"
  title="Toast"
  lede="The corner notice. It confirms something that just happened and leaves on its own."
  note="Four uses, each with its lifetime built in. What has to be read without hurry lives in the bell; what has to be decided lives in a dialog."
/>

<Section
  n="01"
  title="Four uses, four lifetimes"
  intro="The tone is not decoration: it decides how long the notice stays."
>

<div className="grid grid-cols-1 gap-px overflow-hidden rounded-md border border-border-subtle bg-border-divider sm:grid-cols-2">
  <div className="flex flex-col gap-1 bg-panel p-4">
    <Text variant="mono-sm" tone="brand">confirmation · 5 s</Text>
    <Text variant="meta" tone="secondary">success and info, with the countdown at the foot.</Text>
  </div>
  <div className="flex flex-col gap-1 bg-panel p-4">
    <Text variant="mono-sm" tone="brand">attention · stays</Text>
    <Text variant="meta" tone="secondary">warning and danger, until dismissed.</Text>
  </div>
  <div className="flex flex-col gap-1 bg-panel p-4">
    <Text variant="mono-sm" tone="brand">undo · 8 s</Text>
    <Text variant="meta" tone="secondary">neutral, which is how long regret takes.</Text>
  </div>
  <div className="flex flex-col gap-1 bg-panel p-4">
    <Text variant="mono-sm" tone="brand">in progress · until it finishes</Text>
    <Text variant="meta" tone="secondary">progress, then it becomes its own outcome.</Text>
  </div>
</div>

<Canvas of={ToastStories.Tones} sourceState="none" />

<Rule>
  If a piece of data vanishes with the notice and exists nowhere else, that is
  a design fault, not a feature this component lacks.
</Rule>

</Section>

<Section
  n="02"
  title="Undo"
  intro="The one action a toast is allowed to carry, and the reason the neutral tone lives eight seconds instead of five."
>

<Canvas of={ToastStories.UndoAction} sourceState="none" />

</Section>

<Section
  n="03"
  title="It can follow a promise"
  intro="One call covers the three states: in progress while it runs, then the outcome it resolved or rejected into."
>

<Canvas of={ToastStories.FollowsAPromise} sourceState="none" />

<P>Which is what keeps an export from needing three call sites and a piece of state in the page.</P>

</Section>

<Section n="04" title="Where it is used">

<UsedIn>
  <Place at="Resolving an issue">success, with undo.</Place>
  <Place at="Exports">progress, then the download.</Place>
  <Place at="Failed deliveries">danger, which stays until it is read.</Place>
  <Place at="Bulk actions">One notice for the batch, never one per row.</Place>
</UsedIn>

</Section>

<Section n="05" title="What it guarantees">

<Guarantees>
  <Guarantee story="DangerStays">A danger notice never expires on its own.</Guarantee>
  <Guarantee story="Progress">A progress notice holds its place until the work it reports finishes.</Guarantee>
  <Guarantee>At most three are stacked; the rest queue rather than covering the corner.</Guarantee>
  <Guarantee>The countdown pauses while the pointer is over the notice.</Guarantee>
</Guarantees>

</Section>

<Section n="06" title="Props">

<Controls />

</Section>

</DocPage>
</Unstyled>
