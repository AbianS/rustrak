---
"docs": patch
---

Clarify in the source maps and troubleshooting docs that source map upload goes to `sentry.io` by default and must be pointed at the Rustrak server via the plugin's `url`/`authToken`.
